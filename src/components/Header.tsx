import MainLogo from "./logo/MainLogo";

export default function Header() {
	return (
		<header className="flex-no-wrap fixed top-0 flex w-full bg-[#ffffff] px-5 py-5">
			<MainLogo />
		</header>
	);
}
